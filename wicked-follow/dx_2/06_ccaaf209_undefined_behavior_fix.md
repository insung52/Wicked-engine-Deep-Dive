# 커밋 #6: ccaaf209 - Get rid of some undefined behaviour

## 기본 정보

| 항목 | 내용 |
|------|------|
| 커밋 해시 | `ccaaf209` |
| 날짜 | 2025-11-17 |
| 작성자 | Turánszki János |
| 카테고리 | 안정성 / 버그수정 |
| 우선순위 | 높음 |

## 변경 파일 요약

| 파일 | 변경 |
|------|------|
| wiGraphicsDevice_DX12.cpp | `CopyTexture()`에서 잘못된 캐스팅 제거 |
| wiGraphicsDevice_Vulkan.cpp | 동일 |

---

## 배경 지식: C++ 타입 캐스팅과 Undefined Behavior

### C-Style 캐스팅의 위험성

C-style 캐스팅 `(Type*)ptr`은 거의 모든 캐스팅을 허용합니다.

```cpp
// C-style 캐스팅이 허용하는 것들:
int* p1 = (int*)some_double_ptr;      // reinterpret_cast
const int* p2 = (const int*)p1;       // const_cast
Base* p3 = (Base*)derived_ptr;        // static_cast
void* p4 = (void*)any_ptr;            // 뭐든 가능

// 문제: 컴파일러가 잘못된 캐스팅을 경고하지 않음!
```

### Undefined Behavior (UB)란?

**Undefined Behavior**는 C++ 표준이 정의하지 않은 동작입니다.

```
┌─────────────────────────────────────────────────────────────────┐
│                  Undefined Behavior 결과                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  UB가 발생하면:                                                 │
│                                                                 │
│  1. 우연히 "정상 동작"                                          │
│     - 메모리 레이아웃이 비슷해서 동작하는 것처럼 보임            │
│     - 다른 컴파일러/플랫폼에서 깨질 수 있음                     │
│                                                                 │
│  2. 잘못된 결과                                                 │
│     - 쓰레기 값 반환                                            │
│     - 잘못된 메모리 접근                                        │
│                                                                 │
│  3. 크래시                                                      │
│     - Segmentation Fault                                        │
│     - Access Violation                                          │
│                                                                 │
│  4. 보안 취약점                                                 │
│     - 메모리 오염                                               │
│     - 버퍼 오버플로우                                           │
│                                                                 │
│  5. Time Travel (가장 무서운 것)                                │
│     - 컴파일러가 UB를 가정하고 "불가능한" 코드를 제거           │
│     - UB 이전의 코드도 영향받을 수 있음                         │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 문제 상황

### 코드 분석

```cpp
// CopyTexture 함수
void GraphicsDevice_DX12::CopyTexture(
    const Texture* dst,  // Texture 타입!
    // ...
    const Texture* src,  // Texture 타입!
    // ...)
{
    // ⚠️ 문제: Texture*를 GPUBuffer*로 캐스팅!
    auto src_internal = to_internal((const GPUBuffer*)src);
    auto dst_internal = to_internal((const GPUBuffer*)dst);
    // ...
}
```

### 왜 Undefined Behavior인가?

```
┌─────────────────────────────────────────────────────────────────┐
│                   타입 불일치 문제                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  Texture 구조체:                                                │
│  ┌─────────────────────────────────┐                            │
│  │ internal_state (shared_ptr)     │                            │
│  │ desc (TextureDesc)              │ ← Texture 고유 멤버        │
│  │ // ...                          │                            │
│  └─────────────────────────────────┘                            │
│                                                                 │
│  GPUBuffer 구조체:                                              │
│  ┌─────────────────────────────────┐                            │
│  │ internal_state (shared_ptr)     │                            │
│  │ desc (GPUBufferDesc)            │ ← GPUBuffer 고유 멤버      │
│  │ // ...                          │                            │
│  └─────────────────────────────────┘                            │
│                                                                 │
│  문제:                                                          │
│  (const GPUBuffer*)texture_ptr                                  │
│  → Texture 메모리를 GPUBuffer로 해석                            │
│  → 멤버 오프셋이 다를 수 있음                                   │
│  → Undefined Behavior!                                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### C++ 표준에서의 정의

```cpp
// Strict Aliasing Rule (엄격한 앨리어싱 규칙)
//
// 다른 타입의 포인터를 통해 객체에 접근하면 UB
// (일부 예외: char*, unsigned char*, std::byte*)

Texture texture;
GPUBuffer* buffer_ptr = (GPUBuffer*)&texture;  // ⚠️ 캐스팅 자체는 OK

buffer_ptr->desc;  // 💥 UB! GPUBuffer가 아닌 Texture에 접근
```

### 왜 "동작"했는가?

```
┌─────────────────────────────────────────────────────────────────┐
│                  우연히 동작한 이유                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  이 코드가 크래시하지 않은 이유:                                │
│                                                                 │
│  1. internal_state가 첫 번째 멤버                               │
│     - Texture와 GPUBuffer 모두 internal_state가 첫 멤버        │
│     - 오프셋 0에 위치 → 같은 주소 접근                         │
│                                                                 │
│  2. to_internal()이 internal_state만 접근                       │
│     - desc 등 다른 멤버는 접근 안 함                           │
│     - 운 좋게 올바른 데이터 읽음                               │
│                                                                 │
│  하지만 여전히 UB:                                              │
│  - 컴파일러가 최적화 시 이상하게 동작할 수 있음                 │
│  - 구조체 레이아웃 변경 시 깨질 수 있음                         │
│  - 다른 플랫폼에서 다르게 동작할 수 있음                        │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 해결: 올바른 오버로드 사용

### WickedEngine 해결 방식: 템플릿 기반 to_internal

```cpp
// 타입별 내부 타입 매핑
template<typename T> struct DX12Type;
template<> struct DX12Type<GPUBuffer> { using type = Resource_DX12; };
template<> struct DX12Type<Texture> { using type = Texture_DX12; };
template<> struct DX12Type<Sampler> { using type = Sampler_DX12; };
// ...

// 템플릿 기반 to_internal
template<typename T>
typename DX12Type<T>::type* to_internal(const T* param)
{
    return static_cast<typename DX12Type<T>::type*>(param->internal_state.get());
}

// 사용:
auto src_internal = to_internal(src);  // T=Texture → Texture_DX12* 반환
auto dst_internal = to_internal(dst);  // T=Texture → Texture_DX12* 반환
```

### VizMotive 해결 방식: 함수 오버로드

VizMotive는 이미 타입별 오버로드가 있었으므로, 잘못된 캐스팅만 제거:

```cpp
// 기존 오버로드들
Resource_DX12* to_internal(const GPUResource* param)
{
    return static_cast<Resource_DX12*>(param->internal_state.get());
}

Resource_DX12* to_internal(const Texture* param)  // ← Texture 전용 오버로드
{
    return static_cast<Resource_DX12*>(param->internal_state.get());
}

// 수정된 CopyTexture
void GraphicsDevice_DX12::CopyTexture(const Texture* dst, ..., const Texture* src, ...)
{
    // 변경 전: to_internal((const GPUBuffer*)src)  ← 잘못된 캐스팅
    // 변경 후: to_internal(src)  ← 올바른 오버로드 호출
    auto src_internal = to_internal(src);
    auto dst_internal = to_internal(dst);
    // ...
}
```

---

## 비유: 번역기

```
┌─────────────────────────────────────────────────────────────────┐
│                        번역기 비유                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  상황: 한국어 문서를 영어로 번역하고 싶음                        │
│                                                                 │
│  잘못된 방법 (변경 전):                                          │
│  ─────────────────────                                          │
│  "이 한국어 문서를 일본어라고 거짓말하고 일본어→영어 번역기 사용"│
│                                                                 │
│  → 번역기가 일본어라고 믿고 처리                                │
│  → 결과가 이상하거나, 우연히 맞거나, 크래시                     │
│                                                                 │
│  올바른 방법 (변경 후):                                          │
│  ─────────────────────                                          │
│  "이 한국어 문서를 한국어→영어 번역기 사용"                      │
│                                                                 │
│  → 올바른 번역기 사용                                           │
│  → 정확한 결과                                                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## VizMotive 적용 현황

### 적용 완료 ✅

**적용 일자**: 2026-01-28

### 수정 위치

**GraphicsDevice_DX12.cpp:7070-7074**:

```cpp
void GraphicsDevice_DX12::CopyTexture(const Texture* dst, ..., const Texture* src, ...)
{
    CommandList_DX12& commandlist = GetCommandList(cmd);
    auto src_internal = to_internal(src);  // ✅ 올바른 오버로드
    auto dst_internal = to_internal(dst);  // ✅ 올바른 오버로드
    // ...
}
```

### 참고: CopyBuffer는 정상

```cpp
// CopyBuffer는 GPUBuffer*를 받으므로 캐스팅 정상
void GraphicsDevice_DX12::CopyBuffer(const GPUBuffer* pDst, ..., const GPUBuffer* pSrc, ...)
{
    auto src_internal = to_internal((const GPUBuffer*)pSrc);  // OK
    auto dst_internal = to_internal((const GPUBuffer*)pDst);  // OK
    // ...
}
```

사실 이 캐스팅도 불필요하지만 (이미 GPUBuffer*), UB는 아님.

---

## 요약

| 항목 | 내용 |
|------|------|
| 문제 | `CopyTexture()`에서 `Texture*`를 `GPUBuffer*`로 C-style 캐스팅 |
| 위험 | Strict Aliasing Rule 위반 → Undefined Behavior |
| 증상 | 우연히 동작하거나, 최적화 시 이상 동작, 다른 플랫폼에서 크래시 |
| 해결 | 올바른 타입의 `to_internal()` 오버로드 호출 |
| VizMotive | ✅ 적용 완료 (2026-01-28) |

### 핵심 교훈

> **타입 캐스팅은 신중하게**
>
> C-style 캐스팅 `(Type*)ptr`은 거의 모든 것을 허용하므로 위험.
> 다른 타입으로 캐스팅 후 멤버 접근은 Undefined Behavior.
>
> 올바른 해결책:
> - 함수 오버로드로 타입별 처리
> - 템플릿으로 타입 안전한 처리
> - `static_cast`, `dynamic_cast` 사용하여 의도 명확히
